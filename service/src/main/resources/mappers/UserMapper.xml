<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="me.licq.javademo.dao.UserDAO">

    <select id="getUserByLoginId" resultType="me.licq.javademo.domain.User" parameterType="java.lang.String">
        select * from user where loginid=#{value}
    </select>

</mapper>